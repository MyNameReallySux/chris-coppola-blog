import { expect } from 'chai'

import { getPosts } from '../client/core'

describe('Application Logic', () => {
	describe('getPosts', () => {
		it('returns an array of posts', function(){
//			let state = Map()
//			try {
//				let nextState = await getPosts(state)
//				let posts = nextState.get('posts')
//				console.log(posts)
//			} catch (error){
//				console.log(error)
//			}
		})
	})
})